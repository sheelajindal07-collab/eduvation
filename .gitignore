# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
build/
dist/

# Env / secrets — never commit real values (CLAUDE.md non-negotiable)
.env
.env.local
.env.*.local
.env.test

# Node (if Playwright/e2e tooling adds a package.json later)
node_modules/

# Migration owner's own second local Supabase stack (supabase/config.toml
# "SECOND STACK FOR THE MIGRATION LANE"; CONSENT-4 is the first to open
# it) — scratch, per-worktree, never shared/tracked.
.supabase-migration/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log

# BCI-002 — M1 first vertical slice

**Milestone:** M1 · **Week:** 2–3 · **Status:** DONE — explore → compare
verified end-to-end against the real Mumbai Supabase project

## Scope (per Lite Build Pack §9)
"Published career record → explore → compare two options, on synthetic
fixtures." Exit proof required: real staging database and browser journey
pass; Lite Build Pack §9 usability round 1 on the clickable prototype.

## Progress so far (2026-09-19)
- **Schema written**: `db/migrations/0001_init.sql` — `sources`,
  `careers`, `pathways`, `claims` (full provenance/status model per
  `docs/DATA.md`), `student_profiles`, `reviewers`. A DB trigger forbids
  ever publishing a synthetic-sourced claim (not just app-level policy).
- **RLS policies written**: world-readable public knowledge base,
  reviewer-only writes, strictly own-row `student_profiles`. Explicitly
  scoped as an M1 baseline — the stricter "author cannot approve their
  own claim" rule is deferred to M4 (noted inline in the migration).
- **Client wired**: `app/db/client.py` — anon client for guest reads,
  user-scoped client that carries the signed-in user's access token (the
  RLS-enforcement contract from `docs/SECURITY.md`). No owner/service-role
  path exists in application code at all.
- **RLS tests written for real**: `tests/db/test_rls.py` — guest /
  student A / student B / reviewer access matrix, 9 test cases. They
  create and tear down throwaway test users via the service-role key
  (test-only, never read by the app). Currently **skip with a printed
  reason** (verified: ran once, watched them error for a real reason —
  `pytestmark` in a conftest doesn't propagate — fixed with a
  `pytest_collection_modifyitems` hook, ran again, now skip correctly).

## Progress, continued (2026-09-19)
- **Comparison assembly** (`app/planning/comparison.py`): pure functions
  implementing `docs/DATA.md`'s trust-label mapping table exactly, plus
  the three-separate-amounts cost breakdown. Deliberately DB-agnostic (no
  I/O) so it's fully unit-testable without a live project.
- **12 real unit tests** (`tests/unit/test_comparison.py`), one per branch
  of the trust-label table including boundary cases (exact SLA edge,
  missing source, a synthetic source defensively refused even if marked
  published) — the DB trigger in the migration is the real guarantee,
  this is defence in depth. All pass (`pytest tests/unit -q` → 18 passed,
  up from 6 at BCI-001).
- Real ruff/mypy errors were caught and fixed while writing this (an
  awkward `and`-chained expression that mypy correctly flagged as
  possibly returning a `Claim` instead of its `.value`).

## Progress, continued (2026-09-19) — DONE: live verification
- Owner created a real Supabase project (Mumbai, `ap-south-1`), applied
  `db/migrations/0001_init.sql` via the SQL Editor, and set up `.env`
  with the real project values.
- **`make test-db` → 9/9 passing against the live database.** This is
  the actual M1 exit proof from Lite Build Pack §9 ("Guest, student A, student B
  and reviewer access matrix passes") — not a skip, not a mock.
- Two secrets were pasted into chat during setup (a JWT secret, then a
  service-role key); owner was asked to rotate both as a precaution,
  neither was written to any file here.
- `scripts/apply_migrations.py` added (owner asked not to hand-paste
  migrations again): a direct `psycopg` connection driven by a
  `DATABASE_URL` the owner will add to `.env` themselves — not the
  Supabase MCP tool or management API, per the earlier instruction.
  Written, lint/typecheck clean, not yet run (needs `DATABASE_URL`).
- Fixed a real bug this surfaced: `tests/unit/test_health.py`'s
  "unconfigured" test started failing once a real `.env` existed,
  because deleting OS env vars doesn't stop pydantic-settings reading
  the `.env` file directly. Fixed with `Settings(_env_file=None)`.
- Also fixed a stale inconsistency: `app/core/config.py` still said
  `anthropic_api_key` despite the Gemini decision being recorded the
  same day. Renamed throughout.
- 27 tests passing total (18 unit + 9 live DB), lint/typecheck clean.

## Final piece: API routes wired and verified live (2026-09-19)
- `app/api/deps.py`: guest (anon-key) client by default, RLS-scoped
  user client when an `Authorization: Bearer` header is present — no
  owner/service-role path in request-handling code at all.
- `app/api/explore.py`: `GET /careers` — lists published careers and
  pathways. No access logic of its own; RLS on the tables is the only
  enforcement point, by design (one place, not two).
- `app/api/compare.py`: `GET /compare?pathway_id=X&pathway_id=Y` —
  assembles the comparison-field structure using the already-tested
  `app/planning/comparison.py` logic over real claims/sources fetched
  live.
- `tests/db/test_api_explore_compare.py`: 4 real end-to-end tests —
  seeds a career/pathway/claim via the service-role admin client, reads
  back through the actual FastAPI app as an anonymous guest would (no
  auth header). Confirms: a guest sees a seeded career; comparing fewer
  than 2 pathways is rejected; **a draft claim's value never reaches a
  guest response even though the claim row exists** (the core safety
  property of this whole slice); and a claim published against a real
  official source comes back with the correct trust label and source
  URL, end to end through the live database. Ran the full suite twice in
  a row to confirm cleanup leaves no residue — consistent both times.
- Caught and fixed two real issues while building this: ruff's B008
  flagged FastAPI's own `Depends()`/`Query()` idiom as a bug (it isn't —
  added a project-wide ignore with a comment explaining why), and mypy
  correctly flagged the Supabase client's broad JSON-typed `.data`
  needing an explicit cast at the two places raw rows get indexed.
- **31 tests passing total** (18 unit + 13 live DB), lint/typecheck clean,
  pushed to GitHub with CI green.

## Confirmation still open (not blocking, tracked in STATUS.md)
- Pilot state (Gujarat assumed) — blocks the *content track* writing
  real state-specific rules, not any of the engineering above.

## Proposed breakdown
1. **Data foundation**: Supabase schema migration for `Source`, `Claim`,
   `Career`, `Pathway` (append-only migration file, per
   `docs/ARCHITECTURE.md`); RLS policies; restricted application role
   (never the owner/service role) — see `docs/SECURITY.md`.
2. **RLS tests** (`make test-db`): guest/student A/student B/reviewer ×
   read/write, run as the restricted role, not the DB owner.
3. **Explore screen**: list published Career records (synthetic fixtures
   seeded through the same publishing path a real record would use — no
   shortcut that lets fixtures skip maker-checker, even in dev).
4. **Compare screen**: two pathways side by side, fields per
   `docs/UI.md`'s comparison-field list (entry requirements, stages, time
   range, cost as three amounts, evidence with trust labels).
5. **Design track (parallel, Lite Build Pack §5)**: low-fidelity quick-start/
   explore/compare/plan flows; clickable prototype; usability round 1
   (5–8 participants) run *before* this slice's engines are considered
   done — not after.

## Exit proof to produce
- `make test-db` green against a real (staging) Postgres with RLS
  actually enforced (a cross-user read attempt must fail, verified by a
  test that asserts the failure, not just the absence of an error).
- A browser/HTTP walkthrough: seed → publish → explore → compare, entirely
  on labelled synthetic data.
- Usability round 1 notes against the six task criteria in
  `docs/UI.md`/Lite Build Pack §12.

## Reviewer invocations expected during this task
- `data-security-reviewer` once RLS policies and the publishing path exist.
- `ux-qa-reviewer` once explore/compare are reachable end to end.

Not started — waiting on the Supabase provisioning decision above.

# BCI-001 — M0 bootstrap

**Milestone:** M0 · **Week:** 1 · **Status:** complete

## Scope
Implement M0 only, per the Step 1 prompt (amended for the pinned stack,
`docs/BCION-Lite-Build-Pack.md` §14):
1. Identify existing stack and gaps; propose only necessary changes.
2. Create/update memory files: `CLAUDE.md`, `docs/PRODUCT.md`,
   `docs/ARCHITECTURE.md`, `docs/UI.md`, `docs/DATA.md`,
   `docs/SECURITY.md`, `STATUS.md`, `docs/DECISIONS.md`, task backlog.
3. Establish and run real lint, typecheck, test and build commands.
4. Configure specialist agents (read/test-only reviewers).
5. Create the smallest runnable application shell with clearly labelled
   synthetic fixtures.

## What was done
- Git repo initialised (`main` branch), full directory skeleton per
  `docs/ARCHITECTURE.md`.
- DPR source converted to `docs/BCION-DPR-v1.1.md` (clean markdown, tables
  reconstructed) + original `.docx` archived alongside it for provenance.
- All seven memory/doc files written (see list above), each citing the
  specific annex/section it summarises.
- `docs/DECISIONS.md` opened with five dated 2026-09-19 entries covering
  every assumption made without owner confirmation (stack pinned, pilot
  state = Gujarat, AI provider = Anthropic, hosting not yet provisioned,
  named reviewers not yet assigned).
- FastAPI app shell (`app/main.py` + `app/api/health.py`) with a `/healthz`
  endpoint that honestly reports `db_configured` / `ai_configured` rather
  than assuming readiness.
- Typed core models (`app/data/models.py`) matching `docs/DATA.md`'s claims
  schema, trust-label mapping and three-outcome eligibility model.
- Module stubs for `rules/`, `planning/`, `ai/`, `db/` — each a docstring
  naming the milestone it's actually built at, not empty without
  explanation.
- Synthetic fixtures (`tests/fixtures/synthetic_data.py`) explicitly
  labelled SYNTHETIC/Testonia, structurally incapable of being confused
  with real records.
- Two specialist agents configured: `.claude/agents/data-security-reviewer.md`,
  `.claude/agents/ux-qa-reviewer.md` — both read/test-only, both with
  stated non-invocation triggers. The third (AI evaluator) is deferred to
  M5 per the blueprint ("added at M5") — see BCI-00X when M5 starts.
- `pyproject.toml`, `Makefile`, `.gitignore`, `.env.example` (names only,
  no values), `.github/workflows/ci.yml` (lint/typecheck/unit-test; DB and
  e2e jobs commented out until M1+ gives them something to run against).

## Exit proof (commands actually run this session, not claimed)
```
$ ruff check app tests          -> All checks passed!
$ mypy app                      -> Success: no issues found in 12 source files
$ pytest tests/unit -v          -> 6 passed, 2 warnings in 0.25s
$ uvicorn app.main:app (live)   -> GET /healthz -> 200 {"status":"ok",
                                    "db_configured":false,"ai_configured":false}
                                    GET /docs -> 200
```
Ruff initially found 6 real errors (4 `str+Enum` -> `StrEnum` findings, 2
line-length); all fixed and reverified, not suppressed.

## Not done / explicitly deferred
- No live Supabase/Postgres connection (M1).
- No RLS policies, no auth (M1/M3).
- No AI provider calls (M5) — adapter interface itself is also deferred to
  M5, not stubbed early, to avoid over-building ahead of its milestone.
- No Dockerfile (needs a hosting decision first — `docs/DECISIONS.md`).
- No `.claude/agents/` AI evaluator yet (M5).

## Owner decisions still needed (tracked in docs/DECISIONS.md and STATUS.md)
Pilot state confirmation, AI provider confirmation, what's already
provisioned (Supabase/VPS/WhatsApp/domain), named content reviewers.
